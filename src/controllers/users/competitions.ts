import { Request, Response } from "express";
export const getCompetition = async (req: Request, res: Response) => {};
export const participantsCompetitions = async (
  req: Request,
  res: Response
) => {};
